Next
- Load and display JSON from computer.
	c Open file.
	w Convert file to JSON object.
	- Convert string to pixel array.
		- replace '],' with ']**'
		- split at '**'
		- iterate through resulting array
			- replace '),' with ')**'
			- split at '**'
	- Draw pixel array on screen.
- Move drawSavedCanvas into drawing and rename.

Don't forget
- Hotkeys
	- Shift+click to erase, E to switch to eraser tool.
- User initiated check for orphaned colors in used-color palette.
- Dropbox integration for sharing and CLOUD!!!!!! features.
* Use canvas-ToBlob to save entire sprite sheet.

Bugs
- 

Save data structure
--------------------
Sprite sheet
--Metadata
----Name (str)
----Number_of_actions (int)
----Sprite_dimensions ([int, int])
--Action 1
----Metadata
------Name (str)
------Number_of_actions (int)
----Sprite 1 ([(int, int, int)])
----Sprite 2
----Sprite 3
--Action 2
----Metadata
------Name
------Number_of_actions
----Sprite 1
----Sprite 2
----Sprite 3

jsonObj.actions[i].sprites[j].pixel_array